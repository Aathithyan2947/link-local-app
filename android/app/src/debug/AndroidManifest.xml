<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Debug-only: Android blocks cleartext HTTP from API 28, which would break talking to a
         local backend over http://10.0.2.2 (emulator) or a LAN IP (device). Kept out of the
         main manifest so release builds keep the default HTTPS-only policy. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
